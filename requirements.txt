fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
scikit-learn==1.5.2
numpy==2.1.1
requests==2.32.3

# LLM providers - all three are installed so you can switch via LLM_PROVIDER
# without reinstalling. Comment out the two you don't use if you want a
# lighter image.
openai==1.51.0
anthropic==0.36.1
google-generativeai==0.8.3

# Optional, only needed if you switch DATABASE_URL to Postgres:
# psycopg2-binary==2.9.9

# Testing
pytest==8.3.3
httpx==0.27.2
